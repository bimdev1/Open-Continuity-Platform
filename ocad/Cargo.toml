[package]
name = "ocad"
version = "0.1.0"
edition = "2021"

[dependencies]
zbus = { version = "4", features = ["tokio"] }
tokio = { version = "1", features = ["full"] }
liboca = { path = "../liboca" }
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
env_logger = "0.10"
